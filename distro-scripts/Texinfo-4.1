cd $SRCS/texinfo-4.1 &&

./configure --prefix=$IULIX/usr &&
make &&
make install &&
make TEXMF=$IULIX/usr/share/texmf install-tex &&

cd $IULIX &&
tar -czf $PKGS/texinfo-4.1.tgz . &&
rm -r ./* &&

echo Done.

