cd $SRCS/make-3.79.1 &&

./configure --prefix=$IULIX/usr &&
make &&
make install &&
chgrp root $IULIX/usr/bin/make &&
chmod 755 $IULIX/usr/bin/make &&

cd $IULIX &&
tar -czf $PKGS/make-3.79.1.tgz . &&
rm -r ./* &&

echo Done.

