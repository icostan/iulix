cd $SRCS/bind-9.2.1 &&

configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var
make &&
make DESTDIR=$IULIX install &&

cd $IULIX &&
tar -czf $PKGS/bind-9.2.1.tgz . &&
#rm -r ./* &&

echo Done.

